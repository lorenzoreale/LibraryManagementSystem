# Library Management System (LMS)

A modular, enterprise-grade Console Application built with C# and .NET. 
This project demonstrates the principles of Clean Architecture, Dependency Injection and SOLID design.

## Architecture
- **Domain:** Core business rules and interfaces (`Book` entity).
- **Infrastructure:** Data persistence (`JsonBookRepository`).
- **Presentation:** Console UI and Dependency Injection Bootstrapper.